# TetsuClaw Handoff - 2026-05-15

## Current State

- Local repo: `/Users/mkultraman/Desktop/Projects/tyrannojr-github/tetsuclaw-core`
- Live droplet repo: `/root/tetsuclaw-core`
- Droplet: `root@174.138.22.14`
- Branch: `main`
- Last runtime commit on the droplet: `aac184e fix(model-router): align codex exec invocation`
- Live service: `nanoclaw.service` is active

Before this handoff doc was added, the local repo, GitHub `main`, and droplet repo were aligned at `aac184e`. This handoff doc may be one docs-only commit newer on GitHub and does not require a droplet deploy.

## What We Finished

- Consolidated TetsuClaw around the main Telegram context.
- Rolled the review-board personas into the main group context instead of keeping them as a separate active chat surface.
- Added host-mediated GitHub tools and granted broad repo access while keeping `tetsuclaw-core` protected infrastructure. The supported MCP surface is list/view repositories, create repositories, and create/update individual text files in non-protected repositories after an explicit user request.
- Added host-mediated model routing with:
  - `model_status`
  - `model_ask`
- Installed and authenticated all intended model providers without using a separate API-key billing path:
  - Codex/OpenAI via ChatGPT login
  - Claude Code
  - Ollama local models
  - Gemini CLI via Google/Gemini login

## Model Provider State

Default provider order:

```text
codex -> claude -> ollama -> gemini
```

Explicit provider calls are also supported:

```text
codex
claude
ollama
gemini
```

Verified live IPC results:

```text
model_status -> ["codex","claude","ollama","gemini"]
model_ask(auto) -> Codex, "codex-auto-ok"
model_ask(provider=claude) -> "claude-ipc-ok"
model_ask(provider=ollama) -> "router-ok"
model_ask(provider=gemini) -> "gemini-ipc-ok"
```

Current provider versions/auth:

```text
codex login status -> Logged in using ChatGPT
claude --version   -> 2.1.90 (Claude Code)
gemini --version   -> 0.42.0
ollama version     -> 0.24.0
```

Installed Ollama models:

```text
qwen3:0.6b
gemma3:1b
qwen2.5:0.5b
```

Default Ollama model:

```text
OLLAMA_MODEL=qwen3:0.6b
```

## Important Fixes And Gotchas

- Codex CLI `exec` no longer accepts `--ask-for-approval`; router now uses the current supported invocation and `--output-last-message` to avoid returning verbose session logs.
- CLI provider calls now explicitly close stdin. This fixed Claude behavior from the long-running `nanoclaw` service.
- A stale `CLAUDE_CODE_OAUTH_TOKEN` was removed from the live droplet `.env`; it was overriding valid Claude local credentials and causing `Invalid bearer token`.
- Gemini headless auth works after running `NO_BROWSER=true gemini`, selecting Google login, authenticating as `matt@mkultraman.com`, and pasting the auth code back into Termius.
- Codex ChatGPT auth works through the SSH tunnel flow:

```bash
ssh -L 1455:127.0.0.1:1455 root@174.138.22.14
codex login
```

Open the printed auth URL in the Mac browser while the SSH session stays open.

## GitHub Access Policy

TetsuClaw can use host-mediated GitHub tools to list/view repositories, create new repositories, and create/update individual text files in non-protected repositories when explicitly asked. New repos should default to private.

`tetsuclaw-core` is protected infrastructure:

- Read/inspect is allowed.
- Do not create/replace it.
- Do not force-push, delete it, or alter remotes.
- Do not modify secrets or GitHub Actions.
- Do not modify runtime code unless the user explicitly asks for `tetsuclaw-core` maintenance.

## Key Commits From This Session

```text
c1449ef feat(tetsuclaw): add host model provider router
9a355ed fix(model-router): harden cli invocation modes
293aed5 chore(model-router): prefer claude before ollama and gemini
8fc3c77 fix(model-router): close stdin for provider cli calls
aac184e fix(model-router): align codex exec invocation
```

## Resume Here

Next work should move back to product planning:

1. Review other OpenClaw/NanoClaw/Hermes-style forks and adjacent agents.
2. Identify what they can do that TetsuClaw cannot yet do.
3. Map those capabilities against the needs of SME operators in Japan.
4. Produce a feature roadmap for TetsuClaw.
5. Decide whether Gemini should remain fourth or move up after real comparisons.

Useful live checks when resuming:

```bash
ssh root@174.138.22.14 'cd /root/tetsuclaw-core && git log -1 --oneline --decorate && systemctl is-active nanoclaw && codex login status && ollama list'
```

For model IPC, use the TetsuClaw tools:

```text
model_status
model_ask(provider=auto, prompt="...")
model_ask(provider=codex, prompt="...")
model_ask(provider=claude, prompt="...")
model_ask(provider=ollama, prompt="...")
model_ask(provider=gemini, prompt="...")
```
